# ============================================
# 文件加密系统 (jiami) - 依赖配置
# Python 3.10+ 兼容
# ============================================

# ------------------------------------------
# 核心加密库 (必需)
# ------------------------------------------
cryptography>=43.0.0
pynacl>=1.5.0
twofish==0.3.0  # Requires a C compiler when no platform wheel is available

# ------------------------------------------
# 图形界面 (选择一个即可，推荐PyQt6)
# ------------------------------------------
PyQt6>=6.4.0
# PySide6>=6.4.0  # 备选，与PyQt6二选一

# ------------------------------------------
# 图像处理
# ------------------------------------------
Pillow>=10.0.0
# opencv-python>=4.8.0  # 可选，隐写术高级功能

# ------------------------------------------
# 数据处理
# ------------------------------------------
numpy>=1.24.0,<2.0.0

# ------------------------------------------
# 系统工具
# ------------------------------------------
psutil>=5.9.0
watchdog>=3.0.0

# ------------------------------------------
# 日志和配置
# ------------------------------------------
loguru>=0.7.0
pyyaml>=6.0.1

# ------------------------------------------
# 测试框架
# ------------------------------------------
pytest>=7.4.0
pytest-cov>=4.1.0
hypothesis>=6.82.0

# ------------------------------------------
# JSON Schema 验证
# ------------------------------------------
jsonschema>=4.17.0

# ------------------------------------------
# 可选依赖 (按需安装)
# ------------------------------------------
# pandas>=2.0.0        # 数据分析
# py7zr>=0.20.0        # 7z压缩支持
# rarfile>=4.1         # RAR压缩支持
# requests>=2.31.0     # 网络功能
# pyinstaller>=6.0.0   # 打包exe
# numba>=0.58.0        # 性能优化
# bcrypt>=4.0.0        # 密码哈希
# argon2-cffi>=23.0.0  # 密码哈希
