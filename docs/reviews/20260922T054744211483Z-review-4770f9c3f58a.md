# Claude review

- Status: completed
- Target: `4770f9c3f58a540da59cc30fd96a0df2b2e33a55`
- Base: `bcfbce55a833826eee7071e2cacfefe5e6df630e`
- UTC: 20260922T054744211483Z
- Prompt SHA256: `e13a7c6c6524986d37f8f0929ebce0377369eb88b96dfcbf6048a3d6335740a0`
- Scope: committed snapshot only; tools limited to Read/Glob/Grep; no tests executed by Claude.

## 审查结论：无阻塞性问题，以下为需要知悉的正确性/边界细节

**范围**：`tools/dev.py`、`tools/architect.py`、`jiami-remote.code-workspace`、`.gitignore`（`4770f9c` vs `bcfbce5`）。已核实用户提供的验证事实（依赖检查、test-core、doctor 返回码、review 缺 base 返回码），未重复运行测试。

### 建议项（非阻塞）

1. **`tools/dev.py` 对 `uv` 缺失无失败处理**：`doctor`（约第 47–59 行）只检查 venv、`key_injector.py`、`base_decryptor.py`，不检查 `uv` 是否存在；而 `setup`/`lock` 直接用 `shutil.which('uv') or str(Path.home()/'.local/bin/uv')`（约第 29 行）拼出的路径去 `subprocess.call`（`run()`，约第 19–20 行）。若 `uv` 未安装，`subprocess.call` 会抛出未捕获的 `FileNotFoundError`，表现为原始 Python traceback，而不是清晰的错误提示，`doctor` 也不会提前预警这个前置依赖缺失。相比之下 `tools/architect.py` 对 `claude` 可执行文件做了 `Path(claude).is_file()` 检查并用 `parser.error` 给出明确提示，两者处理不对称。建议 `doctor` 增加 `uv` 存在性检查，或在 `setup`/`lock` 里加同样的显式判断。

2. **调试配置与 `test-core` 任务覆盖的测试集不一致**：`jiami-remote.code-workspace` 中 `test-core` 任务运行的是 `tools/dev.py test-core`，对应 `tools/dev.py` 里 `CORE_TESTS`（4 个文件：`test_exceptions.py`、`test_config_manager.py`、`test_exception_properties.py`、`test_config_properties.py`）；但 `launch.configurations` 里 `"jiami: 调试配置与异常测试"` 的 `args` 只写了 `tests/unit/test_exceptions.py tests/unit/test_config_manager.py` 两个文件，漏了两个 property 测试。`docs/remote-development.md` 里 “F5 默认可调试配置与异常测试” 的措辞会让人以为 F5 等价于 `test-core`，实际是子集，属于文档/配置的小不一致，不影响正确性但容易误导。

3. **`.gitignore` 的负排除只覆盖了 `src/decryptor/*.py`，未覆盖 `src/encryptor/key_injector.py`**：`.gitignore:41` 的 `*_decryptor.py` 规则会匹配 `base_decryptor.py`，所以新增的 `!/src/decryptor/*.py`（`.gitignore:75`）是对症的、写法也正确（锚定根路径、置于排除规则之后，符合 git 反向匹配要求，未见目录级屏蔽导致反转失效的经典坑）。但 `key_injector.py` 的文件名从未匹配任何现有忽略规则（不以 `_decryptor.py`/`_decryptor.exe` 结尾），说明它的缺失并非 `.gitignore` 导致，这次的忽略规则修复对它没有任何作用。这不是本次新缺陷，只是提醒：不要误以为这次 `.gitignore` 改动顺带解释或修复了 `key_injector.py` 缺失的原因。

4. **`origin/master` 作为固定 base 未先 `fetch`**：workspace 任务 `"jiami: Claude 审查已提交变更"` 直接用 `--base origin/master`（未传 `--ref`，`tools/architect.py` 默认 `HEAD`），如果本地 `origin/master` 落后于远端，对比出的 diff 会失真而不自知。属于工作流建议，非代码缺陷。

### 已确认设计合理、无需改动

- `tools/architect.py` 中 `git rev-parse --verify --end-of-options <ref>^{commit}`（约第 34–35 行）正确防御了以 `-` 开头的 ref 注入；tar 快照解压时对 `is_relative_to` 路径穿越和非文件/目录成员（symlink/硬链接/设备文件）的双重校验（约第 66–72 行）加上 `filter='data'`，防护到位。
- diff 超过 180KB、参考文档超过 16000 字都会 `parser.error` 提前失败而不是静默截断（约第 47、82–84 行），`--base` 缺失返回码 2、无有效 JSON 时返回 `result.returncode or 1`，失败路径清晰，与已验证的返回码行为一致。
- 权限边界：`--tools Read,Glob,Grep` + `--permission-mode plan` + 剔除 `ANTHROPIC_*`/`CLAUDE_CODE_USE_*` 环境变量，这些确实生效——本次审查本身就是在该约束下（工作目录为临时快照、仅 Read/Glob/Grep 可用）运行的，属于实证。但 `docs/remote-development.md` 已如实声明这只是工具层限制、非操作系统隔离，Read/Glob/Grep 理论上仍可读取运行该脚本账号下快照目录之外的文件——这是文档已披露的已知边界，不是本次引入的新问题，仅重申供你知悉。

以上均可视为“待办清理项”，不阻塞本次“先搭开发底座”的验收目标。

## Codex 处理记录

审查针对 4770f9c3f58a；后续修正由 Codex 完成并验证，未宣称 Claude 已复审后续提交。

- 为 setup/lock 增加 uv 缺失时的明确提示，doctor 同时检查 uv。
- F5 调试范围与 test-core 的四个文件保持一致。
- VS Code 审查任务先 git fetch origin；终端说明加入同一步骤。
- key_injector.py 的缺失原因未确认；忽略规则例外只防止 decryptor 源码再次被误忽略。
